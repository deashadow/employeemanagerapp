**This application runs with the JAVA back end server 'EmployeeManager'
server runs on localhost:8080
Client side is done using Angular CLI
npm install -g @angular/cli
ng new 'name of app'
cd 'name of app'
ng serve to start application

